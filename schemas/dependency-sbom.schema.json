{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DataGovOps Dependency SBOM Profile v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["bomFormat", "specVersion", "version", "metadata", "components", "datagovops_nonclaims"],
  "properties": {
    "bomFormat": {"const": "CycloneDX"},
    "specVersion": {"const": "1.6"},
    "version": {"const": 1},
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["component"],
      "properties": {
        "component": {
          "type": "object",
          "additionalProperties": false,
          "required": ["type", "name", "version"],
          "properties": {
            "type": {"const": "application"},
            "name": {"type": "string", "minLength": 1, "maxLength": 256},
            "version": {"type": "string", "minLength": 1, "maxLength": 64}
          }
        }
      }
    },
    "components": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "name", "version", "purl"],
        "properties": {
          "type": {"const": "library"},
          "name": {"type": "string", "minLength": 1, "maxLength": 256},
          "version": {"type": "string", "minLength": 1, "maxLength": 128},
          "purl": {"type": "string", "pattern": "^pkg:pypi/[a-z0-9._-]+@.+$"}
        }
      }
    },
    "datagovops_nonclaims": {
      "type": "object",
      "additionalProperties": false,
      "required": ["complete_transitive_inventory", "vulnerability_assessment_performed"],
      "properties": {
        "complete_transitive_inventory": {"const": false},
        "vulnerability_assessment_performed": {"const": false}
      }
    }
  }
}
